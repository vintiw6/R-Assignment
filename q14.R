# 14. Convert the vector c(2,0.5,1,2,0.5,1,2,0.5,1) to a vector of only 1s, using a vector of length 3.

# Original vector
Vec <- c(2, 0.5, 1, 2, 0.5, 1, 2, 0.5, 1)

# New Vector
OneVec <- rep(1, length(Vec))
OneVec
